package bg.spring.generated.controller;

import bg.spring.generated.pojo.Author;
import bg.spring.generated.repository.AuthorRepository;
import java.lang.Long;
import java.util.List;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.Pageable;
import org.springframework.data.rest.webmvc.RepositoryRestController;
import org.springframework.data.web.PageableDefault;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.ResponseBody;

/**
 * Generated by bg2 with JavaPoet : SpringSourceController isVersion :false */
@RepositoryRestController
public class AuthorController {
    @Autowired
    private AuthorRepository repository;

    @GetMapping(
            path = "/authors"
    )
    @ResponseBody
    public List<Author> list(@PageableDefault Pageable pageable) {
        return  repository.findAll(pageable).getContent();
    }

    @GetMapping(
            path = "/authors/{id}"
    )
    @ResponseBody
    public Author getOneById(@PathVariable Long id) {
        return  repository.getOne(id);
    }
}
